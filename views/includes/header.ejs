<!DOCTYPE html>
<html>
<head>
    <!-- Setting the title of the page based on a condition using EJS syntax -->
    <title><%= typeof title == "undefined" ? "Home" : title %></title>

    <!-- Including CSS and JavaScript libraries -->
    <link rel="stylesheet" href="<%= request.mainURL %>/public/css/bootstrap.min.css" />
    <link rel="stylesheet" type="text/css" href="<%= request.mainURL %>/public/font-awesome-4.7.0/css/font-awesome.min.css" />
    <link rel="stylesheet" type="text/css" href="<%= request.mainURL %>/public/css/style.css" />
    <script src="<%= request.mainURL %>/public/js/vue.global.js"></script>
</head>
<body>
    <!-- Hidden input field to store the base URL for JavaScript use -->
    <input type="hidden" id="base-url" value="<%= request.mainURL %>" />

    <!-- Defining a JavaScript variable 'months' and 'baseUrl' using plain JavaScript -->
    <script>
        var months = ["January", "February", ...];
        const baseUrl = document.getElementById("base-url").value;
    </script>

    <!-- Defining JavaScript functions and variables using EJS tags -->
    <%
        months = ["January", "February", ...];
        formatBytes = function(bytes, decimals = 2) { ... };
    %>

    <!-- Top navigation bar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <!-- Navigation links with dynamic URLs -->
        <a class="navbar-brand" href="<%= request.mainURL %>">File Transfer</a>
        <!-- Collapsible navigation menu button -->
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <!-- Collapsible navigation menu content -->
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <!-- Conditional rendering of navigation links based on login status -->
                <% if (request.isLogin) { %>
                    <!-- Dropdown menu for logged-in user -->
                <% } else { %>
                    <!-- Navigation links for not logged-in user -->
                <% } %>
                <!-- Search form -->
                <form class="form-inline my-2 my-lg-0" method="GET" action="<%= request.mainURL %>/Search">
                    <!-- Search input and submit button -->
                </form>
            </ul>
        </div>
    </nav>

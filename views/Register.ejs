<!-- This code represents a registration form page for users to sign up for an account. 
The template includes various components such as the header, response alert, and footer using the templating engine's syntax for dynamic content inclusion. 
The form gathers the user's name, email, and password for registration. 
The page structure is organized using Bootstrap classes for responsive design and styling.
Users input their information, and when they click the "Register" button, the form data will be sent to the specified URL using the POST method. -->



<%- include ("includes/header", {
   "title": "Register"
}) %>
<!-- Including the header component with a custom title "Register" -->

<div class="container" style="margin-top: 50px;">
 <!-- Creating a container div with top margin -->
    <%- include ("includes/ResponseAlert", {
        "request": request
    }) %>
 <!-- Including the ResponseAlert component within the container div -->

    <div class="row">
       <!-- Creating a row within the container div -->
        <div class="col-md-12">
            <!-- Creating a column that spans the full width on medium screens -->
            <form action="<%= request.mainURL %>/Register" method="POST">
                <!-- Creating a form element with an action attribute for the registration endpoint and using the POST method -->
                <div class="form-group">
                    <label>Name</label>
                     <!-- Label for the name input field -->
                    <input type="text" placeholder="Enter name" name="name" class="form-control" required />
                      <!-- Name input field with placeholder, name attribute, and the "form-control" class for styling -->
                </div>

                <div class="form-group">
                    <label>Email</label>
                     <!-- Label for the email input field -->
                    <input type="email" placeholder="Enter email" name="email" class="form-control" required />
                        <!-- Email input field with placeholder, name attribute, and the "form-control" class for styling -->
                </div>

                <div class="form-group">
                    <label>Password</label>
                      <!-- Label for the password input field -->
                    <input type="password" placeholder="Enter password" name="password" class="form-control" required />
                     <!-- Password input field with placeholder, name attribute, and the "form-control" class for styling -->
                </div>
                <input type="submit" value="Register" class="btn btn-primary" />
                  <!-- Register button with the "btn" and "btn-primary" classes for styling -->
            </form>
        </div>
    </div>
</div>

<%- include ("includes/footer") %>
<!-- Including the footer component -->
